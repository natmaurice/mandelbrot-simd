#ifndef MANDELBROT_UTILS_H_
#define MANDELBROT_UTILS_H_


#define MIN(a, b) ((a) < (b) ? (a) : (b))
#define MAX(a, b) ((a) > (b) ? (a) : (b))

#define MAX4(a, b, c, d) (MAX(MAX(a, b), MAX(c, d)))


#endif // MANDELBROT_UTILS_H_
